{
  "name": "exhibition",
  "scripts": {
    "lint:markdown": "markdownlint **/*.md",
    "lint:editorconfig": "eclint check $(git ls-files)"
  },
  "devDependencies": {
    "eclint": "^2.8.1",
    "markdownlint-cli": "^0.23.2"
  }
}
